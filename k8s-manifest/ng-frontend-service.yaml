apiVersion: v1
kind: Service
metadata:
  labels:
    svc: ng-svc-frontend
  name: ng-frontend
spec:
  ports:
    - name: "angular-frontend-port"
      protocol: TCP
      port: 80
      targetPort: 3000
  selector:
    app: ng-frontend
status:
  loadBalancer: {}
