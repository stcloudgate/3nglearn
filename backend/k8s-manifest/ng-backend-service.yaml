apiVersion: v1
kind: Service
metadata:
  labels:
    svc: ng-svc-backend
  name: ng-backend
spec:
  ports:
    - name: "node-api-port"
      port: 3000
      targetPort: 80
  selector:
    app: ng-backend
status:
  loadBalancer: {}
